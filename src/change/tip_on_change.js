import React from "react";
import { useState } from "react";

export const TipCalcChange = (props) => {
    const [tip, setTip] = useState(0);

    return(
        <div>
            {/* FINISH TIP ON CHANGE */}
        </div>
    )
}